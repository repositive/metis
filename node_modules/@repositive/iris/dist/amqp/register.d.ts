/// <reference types="node" />
import { Channel } from 'amqplib';
export interface SetupRegisterOpts {
    ch: Channel;
    exchange: string;
    namespace?: string;
}
export interface HandlerOpts {
    payload: Buffer;
}
export interface RegisterOpts {
    pattern: string;
    handler: (params: (HandlerOpts)) => Promise<Buffer | void>;
    namespace?: string;
}
export declare function setupRegister({exchange, ch, namespace}: SetupRegisterOpts): Promise<({pattern, handler}: RegisterOpts) => Promise<void>>;
